import { Routes } from '@angular/router';
import { HabitsPageComponent } from './habits.page';

export const habitsRoutes: Routes = [
  { path: 'metrics-app/habits', component: HabitsPageComponent }
];
